{
  "name": "samhartzell.github.io",
  "version": "1.0.0",
  "private": true,
  "description": "Personal portfolio site for Sam Hartzell",
  "scripts": {
    "test": "npm run test:html && npx playwright test --grep-invert @network",
    "test:html": "html-validate index.html",
    "test:links": "npx playwright test --grep @network",
    "test:visual": "npx playwright test --grep-invert @network",
    "test:all": "npm run test:html && npx playwright test"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.1",
    "@playwright/test": "^1.49.0",
    "html-validate": "^8.27.0"
  }
}
